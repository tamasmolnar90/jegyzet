Webszerverről jön
JSON.parse(data)

Webszerverre küld (JSON-ra alakít)
JSON.stringify(data)

npm init -y

npm install express // https://www.npmjs.com/package/express
npm install nodemon --save-dev // https://www.npmjs.com/package/nodemon
npm install dotenv // https://www.npmjs.com/package/dotenv
npm install cors // https://www.npmjs.com/package/cors
npm install pg // https://www.npmjs.com/package/pg
npm install sqlite3 // https://www.npmjs.com/package/sqlite
npm install bcryptjs // https://www.npmjs.com/package/bcryptjs
npm install jsonwebtoken // https://www.npmjs.com/package/jsonwebtoken
npm install -D vitest // https://www.npmjs.com/package/vitest


"type": "module",
"scripts": {
  "dev": "nodemon src/server.js",
},

///////////////////////////////////
///////////////////////////////////
import express from "express";
import "dotenv/config";

const app = express();

app.get("/", async (req, res) => {
  res.send(`<div">Hello World</div>`);
});

const PORT = process.env.PORT || 8080;

app.listen(PORT, () =>
  console.log(`The server is running: http://localhost:${PORT}`),
);


///////////////////////////////////
///////////////////////////////////
JSON.parse(adat); 	str -> obj
JSON.stringify(adat); 	obj -> str