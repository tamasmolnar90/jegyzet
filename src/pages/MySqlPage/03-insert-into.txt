CREATE TABLE table_name (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(255) NOT NULL,
    age INT NOT NULL,
    email VARCHAR(255) NOT NULL UNIQUE,
    car VARCHAR(255) DEFAULT 'Ford',
    doors INT CHECK (doors < 6),
    manual_date DATE NOT NULL DEFAULT (CURRENT_DATE),
    auto_date CURRENT_DATE,
    date_and_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

////////// RECORD //////////

INSERT INTO table_name (name, age, email, car, doors, date) VALUES
('Teljes Név', 26, 'example@gmail.com', 'Ford', 5, '2026-06-08' vagy CURDATE());



//////// RECORD UNIQUE KEY UPDATE //////////

INSERT INTO table_name (name, age, email, car, doors, date) VALUES
('Teljes Név', 26, 'example@gmail.com', 'Ford', 5, '2026-06-08' vagy CURDATE())
ON DUPLICATED KEY UPDATE
name = VALUES(name),
age = VALUES(age),
email = VALUES(email),
car = VALUES(car),
door = VALUES(door);

Ha az email már létezik, nem hoz létre új sort, de frissíti a többi adatot az ON DUPLICATE KEY UPDATE miatt.
Ha az email nem létezik, simán beszúrja az új sort.